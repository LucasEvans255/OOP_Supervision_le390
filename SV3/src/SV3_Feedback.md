10.1
A sketched UML diagram of each would be useful here in the exam. And in the Strategy case, the behaviour of the program absolutely *should* change.
In the State pattern, the State decides when it transitions to a new State context (and hence when behaviour changes). 

10.2 Additionally this means we have to update all references to the old Lecturer, and replace them with the new Professor. This is the really onerous (and bug prone) part of the process.

The problem with `mRank` is not so much that it needs to be deleted, as that we need to replace all references to it with the new rank. Having the rank as a component of Academic (rather than the -is-a sub-class) means that we do not ever need to pass out this mutable object. We can instead keep a single reference to this safely encapsulated within Academic.

The pattern we can use is `Decorator`. If we want to we can decorate the rank with an immutable wrapper to stop anyone doing damage with it.


10.3 Yes.

11 All good.


__2022 Q3__

a) Yes. You would need to expand *how* bugs are reduced (what type), and what you mean by no side effects. 

b) Yes. This adds lots of additional checking code in the application (ugly and error prone).

The key to this is also to meet the requirement that we can treat all of these as Locations in a single list. Hence if we really do want Mutable/Immutable classes we should put them in a hierarchy.

You have also not ensured that exceptions are thrown if latitude and longitude are not within bounds (and phi/theta are not good names for these). You also do not have any getters/setters.

Overall, this answer would get a pretty low result in an exam.

__2022 Q4__

a) Good, except that you could remove the phrase, "if we have List\<A\>, and".

b) Unnecessary information included. "Type erasure is the process at compilation of deleting all generics and replacing them with Object" is pretty much sufficient for the definition. There is some confusion here about the roles of JVM and compiler. Can discuss in supervision.

c) Incorrect for ii) and iv). See model answers as well for an idea of the level of explanation needed for the [2/2] on each one.

d)

i) *will* compile. This is the pre-Generics Java standard (before Java 5). It is in fact identical to ii) in terms of the generated bytecode, but ii) is clearly preferable for clarity.

For ii), 'it clearly defines the fact that primitives cannot be used in generics' is not a true or relevant statement. Not sure what you meant by this (discuss in supervision).

For iii) the clear distinction is, as you say that it has to be on the RHS of a statement. This is only really clear if you write a line of code to show this:

```java 
Set<Object> data = new TreeSet<>();
```

